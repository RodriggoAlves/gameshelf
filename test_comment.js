const { Pool } = require("pg"); require("dotenv").config({path: ".env.local"}); process.env.NODE_TLS_REJECT_UNAUTHORIZED="0"; const pool = new Pool({ connectionString: process.env.DATABASE_URL, ssl: { rejectUnauthorized: false }}); pool.query(`INSERT INTO "ReviewComment" ("reviewId", "userId", "content") VALUES (12, (SELECT id FROM "User" LIMIT 1), 'teste')`).then(res => { console.log("Comment added"); pool.end(); }).catch(e => { console.error(e); pool.end(); });
